# Ao iniciar a aplica��o, o Hibernate vai buscar dados no BD e, caso
# n�o encontre nada, ir� lan�ar uma mensagem de erro.
# Esta entrada evita que tal mensagem seja enviada.

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

#Banco Local: siscontabil
# porta padrão do postegres: 5432
spring.datasource.url= ${JDBC_DATASOURCE_URL:jdbc:postgresql://localhost:5432/SICOP?user=postgres&password=728040}
#Cria ou atualiza tabelas a partir das anotações JPA
spring.jpa.hibernate.ddl-auto=update
